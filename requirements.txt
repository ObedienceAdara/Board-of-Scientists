# Pinned versions verified against PyPI as of July 2026.
# Previously this file had ZERO version pins (reproducibility risk) and
# listed `langchain`, `langchain-community`, and `langserve` — none of which
# are actually imported anywhere in this codebase (grep confirms it), while
# the real dependencies (langchain-groq, langchain-openrouter, pydantic) were
# missing entirely. `langserve` is additionally deprecated upstream in favor
# of LangGraph Platform and had a published vulnerability in its playground
# endpoint (arbitrary file read, versions 0.0.13-0.0.15) — main.py no longer
# uses it (see main.py's REST API section).
#
# After installing, run `pip freeze > requirements.lock.txt` to capture a
# fully reproducible lockfile including transitive dependencies for your
# environment.

# ── Core LangChain / LangGraph ──────────────────────────────────
langchain-core>=1.2.11,<2.0.0
langgraph>=1.2.9,<2.0.0
langsmith>=0.3.0,<1.0.0

# ── LLM providers (pick one via LLM_PROVIDER in .env; all three installed
#    so switching providers doesn't require a new pip install) ──
langchain-openai>=1.3.5,<2.0.0
langchain-groq>=1.1.2,<2.0.0
langchain-openrouter>=0.2.6,<0.3.0

# ── Tools ────────────────────────────────────────────────────────
langchain-tavily>=0.2.18,<0.3.0

# ── Structured output schemas ────────────────────────────────────
pydantic>=2.7.0,<3.0.0

# ── Web server ────────────────────────────────────────────────────
fastapi>=0.115.0,<1.0.0
uvicorn>=0.34.0,<1.0.0

# ── Config / PDF / reporting ──────────────────────────────────────
python-dotenv>=1.0.1,<2.0.0
reportlab>=4.4.0,<5.0.0
pymupdf>=1.24.0,<2.0.0
pdfplumber>=0.11.4,<1.0.0
requests>=2.32.0,<3.0.0
